# CLAUDE.md

This project's agent guide is **[AGENTS.md](./AGENTS.md)** — read it first for
architecture, the module map, how to extend the code, and the gotchas. HTTP
surface is in [docs/API.md](./docs/API.md); user-facing setup/deploy is in
[README.md](./README.md).

Before claiming done: `pnpm check` (0 errors), `pnpm build`, `pnpm test` — and
validate pipeline changes against a real yt-dlp run.
